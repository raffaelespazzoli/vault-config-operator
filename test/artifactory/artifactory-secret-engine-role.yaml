apiVersion: redhatcop.redhat.io/v1alpha1
kind: ArtifactorySecretEngineRole
metadata:
  name: one-repo-only
spec:
  authentication: 
    path: kubernetes
    role: policy-admin
  path: artifactory/raffa-artifactory
  scopes:
  - 